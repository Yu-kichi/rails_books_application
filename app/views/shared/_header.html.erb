<nav>
  <div class="navbar navbar-expand-lg navbar-light bg-light ">
    <div class="container">
      <div class="navbar-brand"><%= link_to t("Top"), root_path %></div>
  <% if user_signed_in? %> 
    <%#=link_to current_user.name, current_user%> <%#"ここでパスワード変更ページに飛ぶと@userが持っていけないのでエラーになる。パスワードページで表示しないようにするか、消すかdevise controllerを変更して表示できるようにする。"%>
    <%= link_to "ユーザーページ", users_path%>
    <%= link_to t("directory.link.Logout"), destroy_user_session_path, method: :delete, class: "navbar-link" %>
  <%else%>
    <%= link_to "Sign up", new_user_registration_path, class: 'navbar-link'  %> 
    <%= link_to "Login", new_user_session_path, class: 'navbar-link'  %>
  <% end %>
        <% if I18n.locale.to_s == "en" %>
          <%= link_to("日本語にする", locale: "ja") %>
        <% else %>
          <%= link_to("English", locale: "en") %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
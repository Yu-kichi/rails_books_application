<p id = "notice"><%= notice %></p>
<h1><%= t '.Books'%></h1>
<table>
  <thead>
    <tr>
      <th><%= translate_attribute(:title)%></th>
      <th><%= translate_attribute(:author)%></th>
      <th><%= translate_attribute(:memo)%></th>
      <th><%= translate_attribute(:picture)%></th>
      <th><%= translate_attribute(:updated_at )%></th>
      <th colspan = "3"></th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.memo %></td>
        <td><%= book.picture %></td>
        <td><%=l(book.created_at, format: :short)%></td>
        <td><%= link_to t('directory.link.Show'), book %></td>
        <td><%= link_to t('directory.link.Edit'), edit_book_path(book) %></td>
        <td><%= link_to t('directory.link.Destroy'), book, method: :delete, data: { confirm: t('directory.modal.confirm') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to t('.New Book'), new_book_path %>
<%= paginate @books %>

ページ:<%= @books.current_page %><br>
オフセット：<%= @books.offset_value  %><br>
表示されてる件数：<%= @books.count  %><br>
トータル件数：<%= @books.total_count %><br>
最初のページならtrue：<%= @books.first_page?%><br>
